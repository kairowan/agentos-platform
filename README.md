# AgentOS Platform

Buildable AgentOS product code for AOSP 17. The AgentOS manifest checks this
repository out at `vendor/agentos` inside the AOSP source tree.

The current baseline defines a Cuttlefish product and a minimal HOME activity.
Future runtime, broker, UI protocol, policy, and SELinux changes belong here.

