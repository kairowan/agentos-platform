<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.agentos.permission.USE_CAPABILITY_BROKER" />
    <permission
        android:name="com.agentos.permission.DELIVER_VOICE_COMMAND"
        android:protectionLevel="signature" />

    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <receiver
            android:name=".VoiceCommandReceiver"
            android:exported="true"
            android:permission="com.agentos.permission.DELIVER_VOICE_COMMAND">
            <intent-filter>
                <action android:name="com.agentos.shell.action.DELIVER_VOICE_COMMAND" />
                <action android:name="com.agentos.shell.action.INTERRUPT_VOICE_OUTPUT" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
